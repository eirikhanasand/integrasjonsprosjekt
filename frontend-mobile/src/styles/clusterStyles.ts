import { StyleSheet } from "react-native"

export const CS = StyleSheet.create({
    clusterWrapper: {
        borderRadius: 5, 
    },
    // Smaller card
    clusterSmaller: {
        marginHorizontal: 10,
        borderRadius: 20,
    },
})

export default CS
